global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'default'

receivers:
  - name: 'default'
    # ── Uncomment when ntfy is enabled ──────────────────────────────
    # webhook_configs:
    #   - url: 'http://ntfy:80/myalerts'
    #     send_resolved: true

# ── Example alert rules (add to prometheus/rules/) ───────────────────
# groups:
#   - name: homelab
#     rules:
#       - alert: InstanceDown
#         expr: up == 0
#         for: 5m
#         labels:
#           severity: critical
#         annotations:
#           summary: "Instance {{ $labels.instance }} down"
