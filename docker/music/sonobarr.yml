services:
  sonobarr:
    image: ghcr.io/dodelidoo-labs/sonobarr:latest
    env_file:
      - .env
    volumes:
      - ${DATA_PATH}/sonobarr:/sonobarr/config
      - /etc/localtime:/etc/localtime:ro
    restart: unless-stopped
    labels:
      traefik.enable: true
      traefik.http.routers.sonobarr.rule: Host(`sonobarr.${DOMAIN}`)
      traefik.http.routers.sonobarr.entrypoints: websecure
      traefik.http.routers.sonobarr.tls: true
      traefik.http.services.sonobarr.loadbalancer.server.port: 5000
      traefik.docker.network: proxy-net
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true

