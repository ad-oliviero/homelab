http:
  routers:
    lemonade:
      rule: "Host(`lemonade.{{ env "DOMAIN" }}`)"
      entryPoints:
        - websecure
      tls: true
      service: lemonade

  services:
    lemonade:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://192.168.1.120:8000"
