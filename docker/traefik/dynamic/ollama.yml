http:
  routers:
    ollama:
      rule: "Host(`ollama.{{ env "DOMAIN" }}`)"
      entryPoints:
        - websecure
      tls: true
      service: ollama

  services:
    ollama:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://192.168.1.120:11434"
