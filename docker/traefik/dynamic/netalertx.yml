http:
  routers:
    netalertx:
      rule: "Host(`netalertx.{{ env "DOMAIN" }}`)"
      entryPoints:
        - websecure
      tls: true
      service: netalertx

  services:
    netalertx:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://10.0.0.2:20211"

