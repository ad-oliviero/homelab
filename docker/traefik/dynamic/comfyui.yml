http:
  routers:
    comfyui:
      rule: "Host(`comfyui.{{ env "DOMAIN" }}`)"
      entryPoints:
        - websecure
      tls: true
      service: comfyui

  services:
    comfyui:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://10.0.0.4:8188"

