http:
  routers:
    comfyui:
      rule: "Host(`comfyui.{{ env "DOMAIN" }}`)"
      entryPoints:
        - websecure
      tls: true
      service: comfyui

  services:
    comfyui:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://192.168.1.120:8188"

