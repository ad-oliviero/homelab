http:
  routers:
    llamacpp:
      rule: "Host(`llamacpp.oliviero.xyz`)"
      entryPoints:
        - websecure
      tls: true
      service: llamacpp

  services:
    llamacpp:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://192.168.1.120:8080"
