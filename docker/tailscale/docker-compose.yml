services:
  tailscale:
    image: tailscale/tailscale:latest
    hostname: adri-rpi
    devices:
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    command: tailscaled 
    privileged: true
    restart: unless-stopped
